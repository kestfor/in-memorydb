---
metrics:
  port: 8082
  csv_path: results.csv

databases:
  #  name: memcache
  #  host: localhost:11211

  - name: redis
    host: localhost:6379

  - name: lume
    host: localhost:8080

test:
  name: test
  minClients: 50
  clientsStep: 5
  maxClients: 100
  stageIntervalS: 60
  requestDelayMs: 1
  maxKeysNum: 100000
