---
metrics:
  port: 8082
  csv_path: results.csv

databases:
  - name: lume
    host: localhost:8080

  - name: memcached
    host: localhost:11211

  - name: redis
    host: localhost:6379

test:
  name: test
  type: get
  minClients: 50
  clientsStep: 5
  maxClients: 200
  stageIntervalS: 60
  requestDelayMs: 1
  maxKeysNum: 100000
