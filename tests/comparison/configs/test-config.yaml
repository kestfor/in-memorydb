---
metrics:
  port: 8082
  csv_path: results.csv

databases:
  - name: lume
    host: localhost:8080

  - name: redis
    host: localhost:6379

  - name: memcached
    host: localhost:11211

test:
  name: test
  type: set
  minClients: 50
  clientsStep: 5
  maxClients: 200
  stageIntervalS: 60
  requestDelayMs: 1
  maxKeysNum: 100000
