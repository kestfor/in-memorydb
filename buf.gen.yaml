version: v2

managed:
  enabled: true

plugins:
  - local: ["go", "tool", "protoc-gen-go"]
    out: .
    opt:
      - paths=source_relative
  - local: ["go", "tool", "protoc-gen-go-grpc"]
    out: .
    opt:
      - paths=source_relative

inputs:
  - directory: .
